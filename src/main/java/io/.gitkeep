# This is just here so Git keeps the 'io' directory

# Put IO-related code in this package.
# It may depend on core data types, but should require very little (if any) dependency on the UI package.
# Leave that kind of thing to the controllers package, which can depend on both the UI and IO packages.