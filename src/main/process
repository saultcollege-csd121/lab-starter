this is just for my own thinking purposes, thought vomiting etc

MAIN FUNCTIONALITY
big task 1-
displaying the filling-in-of-the-text.
couple of options here that i can think of... One involves an alterable textfield over an unalterable one in a lighter color.
    Whenever a key is hit:
    - identify where the caret is. you only need to alter the text format, worst-case, within the word's range of the caret.
    - speaking of which need to be able to identify the range of a word. just gonna go split by spaces.

the other one involves altering a text field as you provide input, so different characters are set to different states corresponding to diff colours.
- by default all chars are set to grey.
- determine position of caret, or rather
- if key entered is backspace, whatever char you were just beyond is now set back to default.

- if key entered char after caret, it can be turned to black.
- if not a match, the characters within the bounds of the word are turned to red.

wait maybe I don't even need a second text to overlay. Maybe just the text is updated based on input.

god. my brain hurts. how does this get displayed?
text is a shape. do i just display it as an array of chars instead?
OKAY!!!
so.
i can change individual characters ONLY if they are an independent Text item. time to change how the class works.
gave each qchar a property where it preserves its initial text state as a constant.
seemingly was the only way to do so, because text nodes cannot be nested within one another.

big task 2- wrapping the text. I kind of figured this would be a problem but I got distracted.
I do want to have resizeable text in a settings tab so I'm thinking will end up doing some format function based on the settings to make things wrap by word.
thinking of just having each line in a container. will need to do some calculations to split it.


big task 3- that was the scene swapping functionality. Might tweak it to be all in one place again, but for now I want to bang out...


big task 4- the timing/data stuff.
what needs to happen:



on first key stroke, timer.start().
start recording every keystroke.

a completed run can be summed up with a boolean:
(replaceabletext.getIdx() is PAST the length of the text && all characters are correct). at which point timer.stop();

things I think are important:
-this only interaction with UI is the display of the live-timer.

return a rundata record once done.
have a time out function after
-I already have a function that delegates keyevents and a listener used to update the view.
... can I just add the checking there? it's in a controller class...

////////////////reworking.//////////////////////////////////////

what confused me for a bit was the use of a record for the typechar.
Somehow making a new record every time was perplexing to me because I was viewing the text as its own object.
but what's neat about this approach is that the text isn't really an object.


//insecure,